<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AI Survival Kit - Future-Proof Your Career</title>
    <style>
        :root {
            --growth-green: #2E7D32;
            --guidance-blue: #1976D2;
            --neutral-dark: #1F1F1F;
            --card-bg: #2A2A2A;
            --neutral-light: #F5F5F5;
            --action-orange: #F57C00;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: var(--neutral-dark);
            color: var(--neutral-light);
            margin: 0;
            padding: 0;
            line-height: 1.6;
        }

        header {
            background: linear-gradient(90deg, var(--guidance-blue), var(--growth-green));
            padding: 40px 20px;
            text-align: center;
            color: var(--neutral-light);
        }

        header h1 { margin: 0; font-size: 2.5rem; }
        header p { margin: 10px 0 0; font-size: 1.2rem; font-weight: 500; }

        .container {
            max-width: 1000px;
            margin: 0 auto;
            padding: 40px 20px;
        }

        /* API Key Section */
        .api-config {
            background: var(--card-bg);
            padding: 20px;
            border-radius: 12px;
            margin-bottom: 40px;
            border: 1px solid var(--neutral-dark);
            text-align: center;
        }

        input[type="password"] {
            padding: 10px;
            width: 300px;
            border-radius: 6px;
            border: 1px solid var(--neutral-dark);
            background: var(--neutral-dark);
            color: var(--neutral-light);
            margin-right: 10px;
        }

        button {
            padding: 10px 20px;
            border-radius: 6px;
            border: none;
            background: var(--action-orange);
            color: var(--neutral-light);
            font-weight: bold;
            cursor: pointer;
            transition: transform 0.2s;
        }

        button:hover { transform: scale(1.05); }

        /* Job Roles Grid */
        .roles-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }

        .role-card {
            background: var(--card-bg);
            padding: 20px;
            border-radius: 12px;
            border: 1px solid var(--neutral-dark);
            cursor: pointer;
            transition: all 0.3s;
            text-align: center;
        }

        .role-card:hover {
            border-color: var(--growth-green);
            background: var(--card-bg);
        }

        .role-card h3 { margin: 0; color: var(--growth-green); }
        .role-card p { font-size: 0.9rem; opacity: 0.8; }

        /* Assessment Section */
        #assessment-area {
            display: none;
            margin-top: 40px;
            background: var(--card-bg);
            padding: 30px;
            border-radius: 15px;
            border-left: 5px solid var(--growth-green);
        }

        #ai-tutorial {
            background: var(--neutral-dark);
            padding: 20px;
            border-radius: 8px;
            margin-top: 20px;
            white-space: pre-wrap;
        }

        /* Certificate Section */
        #certificate-section {
            display: none;
            text-align: center;
            margin-top: 60px;
            padding: 40px;
            border: 4px double var(--growth-green);
            background: var(--card-bg);
        }

        .cert-title { font-size: 2rem; color: var(--growth-green); margin-bottom: 20px; }

        #typing { font-style: italic; color: var(--guidance-blue); display: none; }
    </style>
</head>
<body>

    <header>
        <h1>AI Survival Kit</h1>
        <p>Your Path from Displacement to Empowerment</p>
    </header>

    <div class="container">
        
        <div class="api-config">
            <h3>🔑 First, Activate Your Kit</h3>
            <p>Enter your Google Gemini API Key to unlock personalized AI training.</p>
            <input type="password" id="web-api-key" placeholder="Paste Gemini API Key here">
            <button onclick="saveKey()">Activate Kit</button>
            <p id="key-status" style="font-size: 0.8rem; margin-top: 10px; color: var(--guidance-blue);"></p>
        </div>

        <section id="roles">
            <h2 style="text-align: center;">1. Select Your Current Field</h2>
            <p style="text-align: center; opacity: 0.7;">Click a role to see your AI Risk Assessment and start your survival tutorial.</p>
            
            <div class="roles-grid">
                <div class="role-card" onclick="assessRole('Customer Support', 'High', 'AI Chatbots and Automated Ticketing')">
                    <h3>🎧 Support</h3>
                    <p>Customer Service & Ops</p>
                </div>
                <div class="role-card" onclick="assessRole('Data Entry', 'Critical', 'Automated Data Extraction & Processing')">
                    <h3>⌨️ Data Entry</h3>
                    <p>Clerical & Admin</p>
                </div>
                <div class="role-card" onclick="assessRole('Copywriting', 'High', 'Generative Text & Content Creation')">
                    <h3>✍️ Writing</h3>
                    <p>Content & Marketing</p>
                </div>
                <div class="role-card" onclick="assessRole('Graphic Design', 'Moderate', 'AI Image Generation & Editing')">
                    <h3>🎨 Design</h3>
                    <p>Visual Arts & Media</p>
                </div>
                <div class="role-card" onclick="assessRole('Accounting', 'Moderate', 'Automated Bookkeeping & Auditing')">
                    <h3>📊 Finance</h3>
                    <p>Accounting & Analysis</p>
                </div>
                <div class="role-card" onclick="assessRole('Legal Assistant', 'High', 'Document Review & Research')">
                    <h3>⚖️ Legal</h3>
                    <p>Paralegal & Research</p>
                </div>
                <div class="role-card" onclick="assessRole('Sales', 'Low', 'Personalized Outreach & CRM Automation')">
                    <h3>🤝 Sales</h3>
                    <p>Outreach & Relationships</p>
                </div>
                <div class="role-card" onclick="assessRole('Software Dev', 'Moderate', 'Code Generation & Debugging')">
                    <h3>💻 Coding</h3>
                    <p>Junior Dev & QA</p>
                </div>
            </div>
        </section>

        <section id="assessment-area">
            <h2 id="role-title">Assessment</h2>
            <p id="risk-level"></p>
            <hr style="border: 0; border-top: 1px solid var(--neutral-dark);">
            <h3>🎓 Your Personalized AI Survival Tutorial</h3>
            <p id="typing">AI is generating your custom training...</p>
            <div id="ai-tutorial">Select a role above to begin.</div>
            
            <div style="margin-top: 30px; text-align: center;">
                <button id="cert-btn" style="display: none;" onclick="showCert()">I've Completed My Training</button>
            </div>
        </section>

        <section id="certificate-section">
            <div class="cert-title">CERTIFICATE OF AI ADAPTABILITY</div>
            <p>This certifies that the user has successfully completed the</p>
            <h2 style="color: var(--neutral-light);">AI SURVIVAL & EMPOWERMENT COURSE</h2>
            <p>for the field of <span id="cert-role" style="color: var(--growth-green); font-weight: bold;"></span></p>
            <p style="margin-top: 40px; font-size: 0.8rem; opacity: 0.6;">Verified by AI Survival Kit | Jan 31, 2026</p>
            <button onclick="window.print()" style="margin-top: 20px; background: var(--guidance-blue);">Print / Save as PDF</button>
        </section>

    </div>

    <script>
        let apiKey = localStorage.getItem('survival_kit_key') || '';
        if(apiKey) {
            document.getElementById('web-api-key').value = apiKey;
            document.getElementById('key-status').innerText = "✅ Kit Activated!";
        }

        function saveKey() {
            const key = document.getElementById('web-api-key').value.trim();
            if(key) {
                localStorage.setItem('survival_kit_key', key);
                apiKey = key;
                document.getElementById('key-status').innerText = "✅ API Key Saved & Activated!";
            }
        }

        async function assessRole(role, risk, threat) {
            if(!apiKey) {
                alert("Please enter your API Key first to unlock the AI Tutorial!");
                window.scrollTo(0,0);
                return;
            }

            const area = document.getElementById('assessment-area');
            const tutorial = document.getElementById('ai-tutorial');
            const typing = document.getElementById('typing');
            
            area.style.display = 'block';
            document.getElementById('role-title').innerText = `Role: ${role}`;
            document.getElementById('risk-level').innerHTML = `<b>Risk Level:</b> ${risk} <br> <b>Primary Threat:</b> ${threat}`;
            
            tutorial.innerText = "";
            typing.style.display = 'block';
            area.scrollIntoView({ behavior: 'smooth' });

            try {
                const response = await fetch(`https://generativelanguage.googleapis.com/v1/models/gemini-2.5-flash:generateContent?key=${apiKey}`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        contents: [{ parts: [{ text: `You are an AI Career Educator. Create a short, 3-step "Survival Tutorial" for a ${role} professional. Focus on: 1. One AI tool they should start using TODAY. 2. One skill AI cannot replace in their role. 3. How to use AI to do their current job 2x faster. Keep it encouraging and under 300 words.` }] }],
                        generationConfig: { temperature: 0.7, maxOutputTokens: 1000 }
                    })
                });

                const data = await response.json();
                typing.style.display = 'none';
                
                if(data.candidates && data.candidates[0].content) {
                    tutorial.innerText = data.candidates[0].content.parts[0].text;
                    document.getElementById('cert-btn').style.display = 'inline-block';
                    document.getElementById('cert-role').innerText = role;
                } else {
                    throw new Error("API Error");
                }
            } catch (e) {
                typing.style.display = 'none';
                tutorial.innerText = "❌ Error connecting to AI. Please check your API key and try again.";
            }
        }

        function showCert() {
            document.getElementById('certificate-section').style.display = 'block';
            document.getElementById('certificate-section').scrollIntoView({ behavior: 'smooth' });
        }
    </script>
</body>
</html>
